name: Continuous Integration

on:
  workflow_call:
    
jobs:
  super-linter:
    name: Super Linter
    uses: dmamulashvili/webapi-msa-template/.github/workflows/super-linter.yml@main
  dotnet:
    name: .NET
    needs: [super-linter]
    uses: dmamulashvili/webapi-msa-template/.github/workflows/dotnet.yml@main

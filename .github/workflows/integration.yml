name: Continuous Integration

on:
  workflow_call:
    
jobs:
  codeql:
    name: CodeQL
    uses: dmamulashvili/webapi-msa-template/.github/workflows/codeql.yml@main
  linter:
    name: Linter
    needs: [codeql]
    uses: dmamulashvili/webapi-msa-template/.github/workflows/linter.yml@main
  dotnet:
    name: .NET
    needs: [linter]
    uses: dmamulashvili/webapi-msa-template/.github/workflows/dotnet.yml@main
